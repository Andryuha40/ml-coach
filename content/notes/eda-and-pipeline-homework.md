# Домашнее задание: EDA на данных о студентах (homework 1, basic)

Источник: `content/ml_basics_course/homeworks/homework 1 (Pandas and EDA)/homework_01_(basic).ipynb`.
Все задания имеют равный вес. Задание отрабатывает практику EDA на новом
датасете (в отличие от материала в конспекте, где EDA разбирался на данных
об оттоке клиентов телекома и о цене жилья) плюс слияние нескольких таблиц
через `pandas` — то, чего в конспекте нет.

## Описание данных

В данных есть информация о студентах, разбитая на 10 групп. Файлы делятся
на две категории:
- `Students_info_i` — информация о студентах из группы `i`;
- `Students_marks_i` — оценки студентов из группы `i` за экзамены.

Данные скачиваются архивом:

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
import numpy as np

!wget https://github.com/evgpat/datasets/raw/main/students.zip
!unzip students.zip
```

Предполагается, что далее нужно будет предсказывать оценку по математике
для каждого студента на основе остальных характеристик.

Подсказка про методы слияния таблиц в pandas (аналог SQL `merge`/`join`/
`concat`): https://www.kaggle.com/residentmario/renaming-and-combining#Combining

## Задание 1

1.1. Соберите всю информацию о студентах в одну таблицу `data`: в
итоговой таблице должна быть информация и оценки всех студентов из всех
10 групп. Напечатайте несколько строк таблицы для демонстрации результата.

1.2. Напишите, зачем нужно сделать EDA. Предположите, какую информацию и в
каком виде, исходя из имеющегося датасета и с учётом дальнейшей задачи
предсказания оценки по математике, необходимо посмотреть и с какой целью.

## Задание 2

Удалите столбец `index` у полученной таблицы. Напечатайте первые 10 строк
таблицы.

## Задание 3

3.1. Выведите на экран размеры полученной таблицы.

3.2. Выведите на экран статистические характеристики числовых столбцов
таблицы (минимум, максимум, среднее значение, стандартное отклонение).

3.3. Выведите на экран информацию об остальных признаках.

3.4. Проверьте, есть ли в таблице пропущенные значения.

## Задание 4

Постройте диаграммы распределений числовых признаков.

## Задание 5

5.1. Выведите матрицу корреляций для числовых признаков.

5.2. Прокомментируйте текстом результат.

## Задание 6

Выведите на экран средние баллы студентов по каждому предмету (`math`,
`reading`, `writing`).

Постройте столбчатые диаграммы среднего балла по письму в зависимости от:
- пола (`gender`);
- расовой/этнической группы (`race/ethnicity`);
- уровня образования родителей (`parental level of education`);
- типа обеда (`lunch`);
- прохождения подготовительного курса (`test preparation course`).

## Задание 7

Как зависят оценки от того, проходил ли студент курс подготовки к сдаче
экзамена (`test preparation course`)? Выведите на экран для каждого
предмета в отдельности средний балл студентов, проходивших курс
подготовки к экзамену, и не проходивших курс.

## Задание 8

Переименуйте колонку `parental level of education` в `education`, а
`test preparation course` в `test preparation` с помощью метода pandas
`rename`.

## Задание 9

Зафиксируем минимальный балл для сдачи экзамена: `passmark = 50`.

Ответьте на вопросы:
- Какая доля студентов сдала экзамен по математике (балл > `passmark`)?
- Какая доля студентов, проходивших курс подготовки к экзамену, сдала
  экзамен по математике?
- Какая доля женщин, не проходивших курс подготовки к экзамену, не сдала
  экзамен по математике?

## Задание 10

С помощью `groupby` выполните:
- для каждой этнической группы выведите средний балл за экзамен по чтению;
- для каждого уровня образования выведите минимальный балл за экзамен по
  письму.

## Задание 11

Сколько студентов успешно сдали экзамен по математике? Создайте новый
столбец в таблице `data` под названием `Math_PassStatus` и запишите в него
`F`, если студент не сдал экзамен по математике (балл за экзамен <
`passmark`), и `P` иначе. Посчитайте количество студентов, сдавших и не
сдавших экзамен по математике.

## Задание 12

Переведём баллы в оценки по системе:
- [90; 100] = A
- [80; 90) = B
- [70; 80) = C
- [60; 70) = D
- [50; 60) = E
- [0; 50) = F (Fail)

Создайте вспомогательную функцию `GetGrade(average_mark)`, которая по
среднему баллу за три экзамена выставляет оценку студенту по указанным
критериям. Создайте столбец `Grade` и запишите в него оценку каждого
студента. Выведите количество студентов, получивших каждую из оценок.

```python
def GetGrade(average_mark):
    # ваш код здесь
    ...
```
